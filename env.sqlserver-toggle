# =============================================================================
# SQL Server Environment Toggle Examples
# =============================================================================
# Copy the appropriate section to your .env file based on your needs

# -----------------------------------------------------------------------------
# Option 1: SQL Server ENABLED (Production, Full Database Support)
# -----------------------------------------------------------------------------
SQLSERVER_ENABLED=true
SQLSERVERHOST=192.168.1.200
SQLSERVERPORT=9002
SQLSERVERUSER=sa
SQLSERVERPW=Pass@word123
SQLSERVERDB=images

# -----------------------------------------------------------------------------
# Option 2: SQL Server DISABLED (Development, Faster Startup)
# -----------------------------------------------------------------------------
# SQLSERVER_ENABLED=false
# # No need to configure SQL Server variables when disabled

# -----------------------------------------------------------------------------
# PostgreSQL (Always Available if Configured)
# -----------------------------------------------------------------------------
PGHOST=192.168.1.200
PGPORT=9001
PGDATABASE=images
PGUSER=postgres
PGPASSWORD=your-postgres-password

# -----------------------------------------------------------------------------
# MongoDB (Always Available if Configured)
# -----------------------------------------------------------------------------
MONGO_CONN=mongodb://192.168.1.200:27017
MONGO_USER=your-mongo-user
MONGO_PW=your-mongo-password

# -----------------------------------------------------------------------------
# AWS Configuration (Required for Image Upload)
# -----------------------------------------------------------------------------
AMAZON_KEY_ID=your-aws-access-key
AMAZON_KEY_SECRET=your-aws-secret-key
AMAZON_S3_BUCKET=your-s3-bucket

# -----------------------------------------------------------------------------
# OpenAI Configuration (Required for AI Features)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-your-openai-api-key

# -----------------------------------------------------------------------------
# Datadog Configuration (Optional)
# -----------------------------------------------------------------------------
DD_SERVICE=fastapi-app
DD_ENV=dev
DD_VERSION=1.0
DD_AGENT_HOST=192.168.1.200
DD_TRACE_AGENT_PORT=8126

# Optional: Disable LLM Observability if not needed
DD_LLMOBS_ENABLED=false
DD_LLMOBS_EVALUATORS_ENABLED=false

# -----------------------------------------------------------------------------
# Notion Configuration (Optional)
# -----------------------------------------------------------------------------
NOTION_API_KEY=secret_your-notion-key
NOTION_DATABASE_ID=your-notion-database-id

# -----------------------------------------------------------------------------
# Quick Start Commands
# -----------------------------------------------------------------------------
# 
# 1. Copy this file to .env and uncomment your chosen option:
#    cp env.sqlserver-toggle .env
#
# 2. Check database status:
#    curl http://localhost:8000/api/v1/database-status
#
# 3. Test SQL Server connection:
#    curl http://localhost:8000/test-sqlserver
#
# 4. Use different backends:
#    curl "http://localhost:8000/images?backend=postgres"
#    curl "http://localhost:8000/images?backend=mongo"
#    curl "http://localhost:8000/images?backend=sqlserver"
